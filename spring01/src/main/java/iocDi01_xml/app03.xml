<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

	<!-- 1) 고전적 방법 (직접 new : 소스 재컴파일) -->
	<!-- 생성자에 대한 매개변수가 별도로 없음 : 기본 생성자를 호출한다. -->
	<bean id="sstv" class="iocDi01_xml.SsTVs" lazy-init="true"></bean>
	
	<!-- 2) IOC/DI -> 생성자 주입 -->
	<!-- 인자의 종류와 동일한 생성자를 호출한다. 즉 매개변수의 Type과 Name, 갯수가 동일해야한다. -->
	<!-- 인자를 매칭하는 방법은 1.name속성 활용 2.인덱스 활용이 있다 
		 name 대신 index(0,1,2)로 표시 -->
	<bean id="lgtv" class="iocDi01_xml.LgTVs" lazy-init="true">
		<constructor-arg name="color" value="Gold"/>
		<constructor-arg name="price" value="1234500"/>
		<constructor-arg name="speaker" ref="sp"/>
	</bean>
	<bean id="sp" class="iocDi01_xml.Speaker" lazy-init="true"/>
	
	<!-- 3) IOC/DI -> setter 주입 
   => setter 메서드는 <bean> 생성 직후 컨테이너가 자동 호출
   => 매개변수1개, 2개, 3개 모두 Test
   => setter 찾는 규칙 ( setPrice )  
      -> name에 지정된 값으로 setter메서드명 setSpeaker, setColor 를 찾기때문에
           setter 메서드명과 변수명을 일치시켜야함. 
           ( 그러므로 index는 사용불가 )
           
    기본 생성자를 생성한 후 property 속성을 통해 setter메서드로 찾음
    다만 이 경우 setter메서드를 사용하기때문에 단일값들이 들어가게된다.
    즉 인덱스가 없으므로 인덱스로 값을 찾을 수 없다.
-->   
	<bean id="aitv" class="iocDi01_xml.AiTVs" lazy-init="true">
		<property name="speaker" ref="sp"></property>
		<property name="color" value="silver"></property>
		<property name="price" value="789000"></property>
	</bean>
	
</beans>
